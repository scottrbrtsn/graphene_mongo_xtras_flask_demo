{
  allUsers(filtering: {filters: [{sid_Icontains: "9"}]}) {
    edges {
      node {
        id
        fname
        lname
        loggedIn
        group
        userType
        uid
        sid
      }
    }
  }
}

